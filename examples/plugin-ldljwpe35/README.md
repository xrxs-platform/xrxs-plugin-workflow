# 身份证号校验插件

## 功能说明
在员工信息编辑保存时，后端自动校验身份证号是否以 0 开头。如果以 0 开头则阻止保存并提示「身份证号码不能以 0 开头」。

## 插件类型
后端 Hook（Java）

## 织入点
- employee.field.verify.idcode（员工身份证号校验织入点）

## 校验规则
- 身份证号首位为 '0' → 阻止保存，提示「身份证号码不能以 0 开头」
- 身份证号首位不为 '0' → 正常保存
- 身份证号为空 → 不触发校验
